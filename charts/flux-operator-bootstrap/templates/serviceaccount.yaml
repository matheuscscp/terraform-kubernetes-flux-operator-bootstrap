apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    flux-operator-bootstrap/apply-timestamp: {{ .Values.applyTimestamp | quote }}
